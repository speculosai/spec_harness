{
  "name": "harness-frontend-only-example",
  "version": "0.0.0",
  "private": true,
  "description": "Three guided, frontend-only Speculos Harness demos - the real workspace against an in-browser mock backend.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "check": "node scripts/check.mjs",
    "test": "npm run typecheck && npm run build && npm run check"
  },
  "dependencies": {
    "@speculosai/spec_harness": "^0.1.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^4.3.1",
    "esbuild": "^0.21.0",
    "typescript": "^5.5.0",
    "vite": "^5.3.0"
  }
}
