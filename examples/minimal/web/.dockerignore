# Keep the build context to the sources. A host's node_modules is built for the
# host's platform and would shadow the one installed inside the image.
node_modules
dist
.vite
npm-debug.log*
.env
.env.*
!.env.example
