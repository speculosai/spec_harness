{
  "name": "speculos-harness",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo root for Speculos Harness — an embeddable AI app-building workspace. Pre-release: code lands with v0.1.",
  "license": "Apache-2.0",
  "homepage": "https://speculos.ai",
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "echo \"speculos-harness: builds arrive with the v0.1 code drop\" && exit 0",
    "test": "echo \"speculos-harness: the conformance kit arrives with the v0.1 code drop\" && exit 0"
  }
}
